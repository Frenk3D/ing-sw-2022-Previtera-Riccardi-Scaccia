package it.polimi.ingsw;
//we do not use assume statements because we don't need them, assertTrue gives problems so we use asssertEquals with true
//we need 80% full lines coverage for model and controller, other methods should test automatically also getter and setter
import static org.junit.Assert.assertTrue;

import org.junit.Test;

/**
 * Unit test for simple it.polimi.ingsw.App.
 */
public class AppTest
{
    /**
     * Rigorous Test :-)
     */
    @Test
    public void shouldAnswerWithTrue()
    {
        assertTrue( true );
    }
}
