package it.polimi.ingsw.model.enumerations;

/**
 * This enum is used to manage the state of the controller (if we are in game or not etc...)
 */
public enum GameState {
    LOGIN_STATE,
    SETTING_STATE,
    INGAME_STATE,
    FINISHED_STATE
}
